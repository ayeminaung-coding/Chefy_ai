import { AppRegistry } from 'react-native';
import App from '../App';

AppRegistry.registerComponent('Chefyai', () => App);

AppRegistry.runApplication('Chefyai', {
  rootTag: document.getElementById('root'),
});
